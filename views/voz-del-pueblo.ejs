<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Voz del Pueblo | Detalles</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="icon" href="/img/spider.jpg">
  <link
    rel="stylesheet"
    href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css"
    integrity="sha512-Avb2QiuDEEvB4bZJYdft2mNjVShBftLdPG8FJ0V7irTLQ8Uo0qcPxh4Plq7G5tGm0rU+1SPhVotteLpBERwTkw=="
    crossorigin="anonymous"
    referrerpolicy="no-referrer"
  />
  <style>
    @keyframes fadeInUp {
      from { opacity: 0; transform: translateY(40px); }
      to { opacity: 1; transform: translateY(0); }
    }
    .animate-fadeInUp {
      animation: fadeInUp 1.4s ease-out forwards;
    }
    .animate-delay-100 { animation-delay: 0.1s; }
    .animate-delay-200 { animation-delay: 0.2s; }
    .animate-delay-300 { animation-delay: 0.3s; }
    @keyframes slideIn {
      from { transform: translateX(100%); opacity: 0; }
      to { transform: translateX(0); opacity: 1; }
    }
    .animate-slideIn {
      animation: slideIn 0.6s ease-out forwards;
    }
    @keyframes slideOut {
      from { transform: translateX(0); opacity: 1; }
      to { transform: translateX(100%); opacity: 0; }
    }
    .animate-slideOut {
      animation: slideOut 0.6s ease-out forwards;
    }
    .gradient-text {
      background: linear-gradient(90deg, #2563eb, #c084fc);
      -webkit-background-clip: text;
      background-clip: text;
      color: transparent;
    }
    .hover-pulse {
      position: relative;
      transition: all 0.4s ease;
    }
    .hover-pulse:hover {
      transform: scale(1.05);
    }
    .hover-pulse::after {
      content: '';
      position: absolute;
      inset: 0;
      background: radial-gradient(circle, rgba(37, 99, 235, 0.15) 0%, transparent 60%);
      opacity: 0;
      z-index: -1;
      border-radius: inherit;
      transition: opacity 0.4s ease;
    }
    .hover-pulse:hover::after {
      opacity: 1;
    }
    body {
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
    }
  </style>
</head>
<body class="bg-gradient-to-b from-gray-950 to-black text-gray-100 antialiased selection:bg-blue-600/20">

  <!-- Encabezado -->
  <header class="bg-gray-950 shadow-2xl sticky top-0 z-50 transition-all duration-500">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-10 py-7 flex justify-between items-center">
      <h1 class="text-4xl font-bold gradient-text tracking-tight">Portafolio</h1>
      <!-- Botón hamburguesa -->
      <button id="menu-toggle" class="md:hidden text-gray-200 hover:text-blue-400 focus:outline-none" aria-label="Toggle menu">
        <i id="menu-icon" class="fas fa-bars text-2xl transition-transform duration-400"></i>
      </button>
      <!-- Navegación para pantallas grandes -->
      <nav class="hidden md:flex space-x-14 text-gray-200">
        <a href="/" class="text-base font-medium hover:text-blue-400 transition-colors duration-400 hover-pulse">Inicio</a>
        <a href="/proyectos" class="text-base font-medium text-blue-400 hover-pulse">Proyectos</a>
        <a href="/#contacto" class="text-base font-medium hover:text-blue-400 transition-colors duration-400 hover-pulse">Contacto</a>
      </nav>
      <!-- Menú hamburguesa para pantallas pequeñas -->
      <nav id="mobile-menu" class="hidden md:hidden fixed top-0 right-0 h-full w-80 bg-gray-950 shadow-2xl z-50 p-12">
        <button id="close-menu" class="text-gray-200 hover:text-blue-400 mb-12" aria-label="Close menu">
          <i class="fas fa-times text-2xl transition-transform duration-400"></i>
        </button>
        <div class="flex flex-col space-y-10 text-gray-200">
          <a href="/" class="text-xl font-medium hover:text-blue-400 transition-colors duration-400 hover-pulse">Inicio</a>
          <a href="/proyectos" class="text-xl font-medium text-blue-400 hover-pulse">Proyectos</a>
          <a href="/#contacto" class="text-xl font-medium hover:text-blue-400 transition-colors duration-400 hover-pulse">Contacto</a>
        </div>
      </nav>
    </div>
  </header>

  <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-10 py-28">
    <!-- Introducción -->
    <section class="text-center animate-fadeInUp">
      <h2 class="text-5xl md:text-6xl font-bold gradient-text mb-6 tracking-tight">Voz del Pueblo</h2>
      <p class="text-lg md:text-xl text-gray-300 mb-12 leading-relaxed max-w-3xl mx-auto">
        Un foro en línea diseñado para fomentar la participación ciudadana en discusiones políticas y sociales, permitiendo a los usuarios debatir, votar y proponer ideas de forma democrática y organizada.
      </p>
    </section>

    <!-- Características -->
    <section class="mt-20 animate-fadeInUp animate-delay-100">
      <h3 class="text-3xl font-bold gradient-text mb-8 tracking-tight text-center">Características</h3>
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
        <div class="bg-gray-900/50 rounded-2xl p-6 shadow-2xl hover-pulse">
          <i class="fas fa-user-check text-blue-400 text-2xl mb-3"></i>
          <h4 class="text-xl font-medium text-gray-200 mb-2">Registro y autenticación</h4>
          <p class="text-gray-300 text-base">Acceso seguro para usuarios con registro y autenticación robusta.</p>
        </div>
        <div class="bg-gray-900/50 rounded-2xl p-6 shadow-2xl hover-pulse">
          <i class="fas fa-comment-alt text-blue-400 text-2xl mb-3"></i>
          <h4 class="text-xl font-medium text-gray-200 mb-2">Temas de discusión</h4>
          <p class="text-gray-300 text-base">Crea y participa en temas organizados por áreas de interés.</p>
        </div>
        <div class="bg-gray-900/50 rounded-2xl p-6 shadow-2xl hover-pulse">
          <i class="fas fa-comments text-blue-400 text-2xl mb-3"></i>
          <h4 class="text-xl font-medium text-gray-200 mb-2">Comentarios y respuestas</h4>
          <p class="text-gray-300 text-base">Interactúa con comentarios y respuestas en cada tema.</p>
        </div>
        <div class="bg-gray-900/50 rounded-2xl p-6 shadow-2xl hover-pulse">
          <i class="fas fa-vote-yea text-blue-400 text-2xl mb-3"></i>
          <h4 class="text-xl font-medium text-gray-200 mb-2">Votaciones</h4>
          <p class="text-gray-300 text-base">Vota a favor o en contra de propuestas y comentarios.</p>
        </div>
        <div class="bg-gray-900/50 rounded-2xl p-6 shadow-2xl hover-pulse">
          <i class="fas fa-folder-open text-blue-400 text-2xl mb-3"></i>
          <h4 class="text-xl font-medium text-gray-200 mb-2">Categorías organizadas</h4>
          <p class="text-gray-300 text-base">Explora temas por categorías claras y estructuradas.</p>
        </div>
        <div class="bg-gray-900/50 rounded-2xl p-6 shadow-2xl hover-pulse">
          <i class="fas fa-shield-alt text-blue-400 text-2xl mb-3"></i>
          <h4 class="text-xl font-medium text-gray-200 mb-2">Moderación opcional</h4>
          <p class="text-gray-300 text-base">Herramientas para moderar contenido y mantener discusiones respetuosas.</p>
        </div>
      </div>
    </section>

    <!-- Tecnologías -->
    <section class="mt-20 animate-fadeInUp animate-delay-200">
      <h3 class="text-3xl font-bold gradient-text mb-8 tracking-tight text-center">Tecnologías Usadas</h3>
      <div class="grid grid-cols-1 sm:grid-cols-2 gap-6">
        <div class="bg-gray-900/50 rounded-2xl p-6 shadow-2xl hover-pulse">
          <i class="fas fa-code text-blue-400 text-2xl mb-3"></i>
          <h4 class="text-xl font-medium text-gray-200 mb-2">Node.js + Express</h4>
          <p class="text-gray-300 text-base">Backend robusto para manejar lógica y rutas.</p>
        </div>
        <div class="bg-gray-900/50 rounded-2xl p-6 shadow-2xl hover-pulse">
          <i class="fas fa-database text-blue-400 text-2xl mb-3"></i>
          <h4 class="text-xl font-medium text-gray-200 mb-2">MySQL</h4>
          <p class="text-gray-300 text-base">Base de datos relacional para almacenar datos.</p>
        </div>
        <div class="bg-gray-900/50 rounded-2xl p-6 shadow-2xl hover-pulse">
          <i class="fas fa-file-code text-blue-400 text-2xl mb-3"></i>
          <h4 class="text-xl font-medium text-gray-200 mb-2">EJS</h4>
          <p class="text-gray-300 text-base">Plantillas dinámicas para renderizar vistas.</p>
        </div>
        <div class="bg-gray-900/50 rounded-2xl p-6 shadow-2xl hover-pulse">
          <i class="fas fa-paint-brush text-blue-400 text-2xl mb-3"></i>
          <h4 class="text-xl font-medium text-gray-200 mb-2">Tailwind CSS</h4>
          <p class="text-gray-300 text-base">Estilización moderna y responsive.</p>
        </div>
      </div>
    </section>

    <!-- Capturas -->
    <section class="mt-20 animate-fadeInUp animate-delay-300">
      <h3 class="text-3xl font-bold gradient-text mb-8 tracking-tight text-center">Capturas</h3>
      <div class="grid grid-cols-1 sm:grid-cols-2 gap-8">
        <div class="relative group">
          <img
            src="/img/Captura de pantalla 2025-05-16 095132.png"
            alt="Página principal del foro"
            class="rounded-xl shadow-2xl w-full h-72 object-cover transform transition-all duration-400 group-hover:scale-105 hover-pulse"
          />
          <div class="absolute inset-0 bg-gradient-to-t from-gray-950/50 to-transparent rounded-xl opacity-0 group-hover:opacity-100 transition-opacity duration-400"></div>
          <p class="absolute bottom-4 left-4 text-gray-200 text-sm opacity-0 group-hover:opacity-100 transition-opacity duration-400">Página principal del foro</p>
        </div>
        <div class="relative group">
          <img
            src="/img/Captura de pantalla 2025-05-16 095057.png"
            alt="Comentarios en tema"
            class="rounded-xl shadow-2xl w-full h-72 object-cover transform transition-all duration-400 group-hover:scale-105 hover-pulse"
          />
          <div class="absolute inset-0 bg-gradient-to-t from-gray-950/50 to-transparent rounded-xl opacity-0 group-hover:opacity-100 transition-opacity duration-400"></div>
          <p class="absolute bottom-4 left-4 text-gray-200 text-sm opacity-0 group-hover:opacity-100 transition-opacity duration-400">Comentarios en tema</p>
        </div>
      </div>
    </section>

    <!-- GitHub -->
    <section class="mt-20 text-center animate-fadeInUp animate-delay-300">
      <a
        href="https://github.com/Xanderfx/voz-del-pueblo"
        target="_blank"
        class="inline-block bg-gradient-to-r from-blue-500 to-purple-500 text-white px-12 py-4 rounded-full shadow-2xl hover:from-blue-600 hover:to-purple-600 transition-all duration-400 font-medium text-lg transform hover:scale-105 hover-pulse"
      >
        Ver en GitHub
      </a>
    </section>
  </main>

  <footer class="bg-gray-950 mt-20 py-10 shadow-inner text-center text-gray-400 text-sm">
    © <%= new Date().getFullYear() %> Alexander Liranzo. Todos los derechos reservados.
  </footer>

  <script>
    const menuToggle = document.getElementById('menu-toggle');
    const mobileMenu = document.getElementById('mobile-menu');
    const menuIcon = document.getElementById('menu-icon');
    const closeMenu = document.getElementById('close-menu');

    menuToggle.addEventListener('click', () => {
      const isOpen = !mobileMenu.classList.contains('hidden');
      if (isOpen) {
        mobileMenu.classList.add('animate-slideOut');
        setTimeout(() => {
          mobileMenu.classList.add('hidden');
          mobileMenu.classList.remove('animate-slideOut');
          menuIcon.classList.remove('fa-times');
          menuIcon.classList.add('fa-bars');
        }, 600);
      } else {
        mobileMenu.classList.remove('hidden');
        mobileMenu.classList.add('animate-slideIn');
        menuIcon.classList.remove('fa-bars');
        menuIcon.classList.add('fa-times');
      }
    });

    closeMenu.addEventListener('click', () => {
      mobileMenu.classList.add('animate-slideOut');
      setTimeout(() => {
        mobileMenu.classList.add('hidden');
        mobileMenu.classList.remove('animate-slideOut');
        menuIcon.classList.remove('fa-times');
        menuIcon.classList.add('fa-bars');
      }, 600);
    });

    mobileMenu.querySelectorAll('a').forEach(link => {
      link.addEventListener('click', () => {
        mobileMenu.classList.add('animate-slideOut');
        setTimeout(() => {
          mobileMenu.classList.add('hidden');
          mobileMenu.classList.remove('animate-slideOut');
          menuIcon.classList.remove('fa-times');
          menuIcon.classList.add('fa-bars');
        }, 600);
      });
    });
  </script>

</body>
</html>